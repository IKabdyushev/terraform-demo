---
- name: Ensure docker
  hosts: "all"
  become: true
  roles:
    - geerlingguy.docker
  vars:
    docker_users:
      - ubuntu
      
- name: Install awscli
  hosts: "all"
  become: true
  tasks:
    - name: Install APT
      apt:
        name: awscli
        state: present
